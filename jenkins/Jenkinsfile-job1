pipeline {
   agent none
    stages {
        stage('Stage1') {
             agent any
            steps {
               sh '''
                #!/bin/bash
                pwd
                sleep 10
                ls -lrt  '''
                echo "pull request for pipeline1 folder available"
            }
        }

        stage('Stage2') {
             agent {
        label 'slave1'
    }
            steps {
                sh 'echo this is Stage2'
            }
        }

        stage('Stage3') {
             agent {
        label 'slave2'
    }
            steps {
             sh 'echo this is label Stage3'  
            }
        }
        
    }
}
